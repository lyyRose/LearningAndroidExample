<?xml version="1.0" encoding="utf-8"?>
<LinearLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    xmlns:app = "http://schemas.android.com/apk/res-auto"
    android:id="@+id/activity_news_main"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:orientation="vertical"
    tools:context="com.tangcco.fragmentexample.wangyi.NewsMainActivity">

    <android.support.v7.widget.Toolbar
        android:layout_width="match_parent"
        android:layout_height="?attr/actionBarSize"
        app:logo="@mipmap/tool_bar_logo"
        android:background="?attr/colorPrimary"
        android:minHeight="?attr/actionBarSize"
        android:id="@+id/toolbar" />

    <FrameLayout
        android:layout_width="match_parent"
        android:layout_weight="1"
        android:layout_height="0dp">

    </FrameLayout>

    <RadioGroup
        android:id="@+id/rg_menu"
        android:layout_width="match_parent"
        android:paddingTop="6dp"
        android:paddingBottom="6dp"
        android:layout_height="wrap_content"
        android:orientation="horizontal">

        <RadioButton
            style="@style/rb_menu_style"
            android:text="新闻"
            android:checked="true"
            android:id="@+id/rb_news"
            android:drawableTop="@drawable/rb_tab_news_bg_selector"
            />

        <RadioButton
            style="@style/rb_menu_style"
            android:text="直播"
            android:id="@+id/rb_live"
            android:drawableTop="@drawable/rb_tab_live_bg_selector"
            />

        <RadioButton
            style="@style/rb_menu_style"
            android:text="话题"
            android:id="@+id/rb_topic"
            android:drawableTop="@drawable/rb_tab_topic_bg_selector"
            />

        <RadioButton
            android:id="@+id/rb_personal"
            style="@style/rb_menu_style"
            android:text="我"
            android:drawableTop="@drawable/rb_tab_personal_bg_selector"
            />
    </RadioGroup>
</LinearLayout>
